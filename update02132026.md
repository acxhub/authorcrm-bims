# Author CRM – Development Update Summary

**Date:** February 13, 2026  
**Scope:** Remediation plan implementation, commission system deployment, environment configuration.

---

## 1. Security & Configuration

### 1.1 Supabase credentials via environment variables

- **Files:** `src/integrations/supabase/client.ts`, `src/lib/api/users.ts`
- **Change:** Replaced hardcoded Supabase URL and keys with `import.meta.env.VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, and `VITE_SUPABASE_SERVICE_ROLE_KEY`. Fallbacks kept so the app runs without `.env` (e.g. in previews).
- **New:** `.env.example` with the three variables and usage notes; `.gitignore` updated to exclude `.env`, `.env.local`, `.env.*.local`.
- **Action for deploy:** Copy `.env.example` to `.env`, set real values, and rotate the service role key in Supabase if it was ever committed.

### 1.2 Admin client usage in user delete

- **File:** `src/lib/api/users.ts`
- **Change:** Profile deactivation in `delete()` now uses `adminClient` instead of `supabase`, so it works correctly with RLS.

---

## 2. Database & schema

### 2.1 Leads: `assigned_at` (BUG-002)

- **Migration:** `add_assigned_at_to_leads` (Supabase MCP applied)
- **Change:** Added `assigned_at` (timestamptz) to `public.leads`, backfilled from `updated_at` where `assigned_to` is set.
- **Code:** `src/lib/api/leads.ts` – `assignLead()` sets `assigned_at` on assign/clear; `createLead()` sets it when `assigned_to` is present. Default list order: `assigned_at DESC NULLS LAST`, then `created_at DESC`.
- **Types:** `src/integrations/supabase/types.ts` – `assigned_at` added to leads Row/Insert/Update.

### 2.2 Leads & profiles: type sync with DB

- **File:** `src/integrations/supabase/types.ts`
- **Leads:** Added `recycled_at`, `recycled_by`, `previous_assignee`, `assigned_at` and FKs for `recycled_by` / `previous_assignee`.
- **Profiles:** Added `can_delete_leads` to Row/Insert/Update.
- **Note:** `source` was not in the DB; it was not added to types (no BUG-001 code change needed).

### 2.3 Publisher data cleanup (BUG-003)

- **Migration:** `clean_publisher_invisible_chars` (Supabase MCP applied)
- **Change:** Normalized `publisher` on `public.leads` (trim, collapse whitespace) to fix duplicates caused by invisible characters.

---

## 3. MVP bugs & features

### 3.1 BUG-002 – Lead distribution timestamp/sorting

- Implemented via §2.1. Newly assigned leads sort by `assigned_at` and appear at the top.

### 3.2 BUG-003 – Publisher search

- Search already used `publisher` in the query; migration above improves data quality for better matches.

### 3.3 BUG-004 – Sales users can add leads

- No code change. RLS already allows sales INSERT on `leads`. Recommend manual test: log in as sales and create a lead.

### 3.4 FR-002 – Recycle lead (clear tags, use DB function)

- **File:** `src/components/leads/ForRecycleButton.tsx`
- **Change:** Replaced custom “assign to Lead Sup + add tag” flow with `supabase.rpc('recycle_lead', { lead_id })`, then clearing tags via `removeTags`. Removed hardcoded user ID and debug logs.
- **DB:** `recycle_lead(lead_id)` already existed: sets `assigned_to` NULL, status to Unassigned, `recycled_at` / `recycled_by` / `previous_assignee`, and logs activity.

---

## 4. Data consistency & real-time

### 4.1 Query key alignment

- **Files:** `src/hooks/useDealsRealtime.ts`, `src/hooks/useActivitiesRealtime.ts`, `src/hooks/useCommentsRealtime.ts`
- **Change:** Realtime invalidation keys aligned with hooks: deals use `['deals', id]`; activities use `['activities', 'lead', leadId]`; comments use `['comments', 'lead', leadId]`. Removed debug `console.log` in these and other realtime hooks.

### 4.2 Cascade invalidation

- **Files:** `src/hooks/useStatuses.ts`, `src/hooks/useTags.ts`, `src/hooks/useUsers.ts`
- **Change:** On status/tag create/update/delete, also invalidate `['leads']` (and `['deals']` for status). On user update, invalidate `['leads']` and `['deals']` so lists reflect name/role changes.

---

## 5. Error handling & UX

### 5.1 Deals mutations – error toasts

- **File:** `src/hooks/useDeals.ts`
- **Change:** Added `onError` with destructive toast for `useCreateDeal`, `useUpdateDeal`, `useDeleteDeal`, `useAssignDeal`, `useUpdateDealStatus`.

### 5.2 Toast auto-dismiss

- **File:** `src/hooks/use-toast.ts`
- **Change:** `TOAST_REMOVE_DELAY` set from `1000000` to `5000` ms.

### 5.3 Sold dashboard date preset

- **File:** `src/pages/SoldDashboard.tsx`
- **Change:** Week preset no longer mutates shared `today`; uses a copy for the 7-day offset.

### 5.4 Console cleanup

- Removed debug `console.log` from: `useAuth.tsx`, `ProtectedRoute.tsx`, `AdminPanel.tsx`, `QuickActions.tsx`, `useLeadsRealtime.ts`, `useStatusesRealtime.ts`, `useTagsRealtime.ts`, `useDealsRealtime.ts`, `useActivitiesRealtime.ts`, `useCommentsRealtime.ts`, `useCommissionsRealtime.ts`. Left `console.error` in catch blocks for diagnostics.

---

## 6. Accessibility & types

### 6.1 Aria labels

- **Files:** `src/pages/Index.tsx`, `src/pages/LeadsManagement.tsx`, `src/pages/Auth.tsx`
- **Change:** Added `aria-label` to Export/Notifications, Import/Manage Statuses, and Auth password visibility toggle.

### 6.2 TypeScript

- Realtime hooks: payloads typed (e.g. `payload.new as { id?: string }`) instead of `any`.
- **File:** `src/lib/api/leads.ts` – lead tag mapping uses `{ tag?: Tables<'tags'> }` instead of `any`.

---

## 7. Commission system (DB)

### 7.1 Migration: create_commission_tables

- **Tables:** `commission_templates`, `commission_tiers`, `agent_commission_settings`, `commissions`, `commission_audit_log`, `company_revenue`.
- **Indexes, RLS, triggers:** RLS enabled on all; policies for leads_manager manage, agents/managers read; `update_updated_at_column()` triggers on templates, agent_commission_settings, commissions.

### 7.2 Migration: fix_commission_rls_and_add_safeguards

- **Policies:** Commissions, audit_log, and company_revenue INSERT relaxed to any authenticated user (for deal→Closed Won and system-created rows).
- **Unique index:** `idx_commissions_deal_agent_unique` on `(deal_id, agent_id)`.
- **Trigger:** `ensure_single_default_template` so only one commission template can be default.

---

## 8. Contextual reference

| Item | Location / reference |
|------|----------------------|
| Remediation plan | `.cursor/plans/codebase_issues_remediation_d92f40b6.plan.md` |
| MVP bugs / FRs | `mvpbugs.md` |
| Env template | `.env.example` |
| Supabase project | `https://rvxyycuukrkjlmaytqok.supabase.co` |
| Migrations (recent) | `add_assigned_at_to_leads`, `clean_publisher_invisible_chars`, `create_commission_tables`, `fix_commission_rls_and_add_safeguards` |

---

## 9. Recommended follow-ups

- Rotate service role key if it was ever in git; set new value in `.env`.
- Manually test sales lead creation (BUG-004).
- Optional: fix `leads_with_author_name` SECURITY DEFINER view; add DELETE policies for deals/comments/activity_logs where desired; enable leaked password protection and MFA in Supabase.
